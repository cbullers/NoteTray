<UserControl x:Class="NoteTray.Views.NoteListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d">

    <Grid>
        <!-- Empty state -->
        <TextBlock Text="No notes yet. Click + to create one."
                   Foreground="{StaticResource TextMuted}"
                   FontSize="13"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Margin="20">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Notes.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Notes list -->
        <ListBox x:Name="NoteList"
                 ItemsSource="{Binding Notes}"
                 SelectedItem="{Binding SelectedNote}"
                 Style="{StaticResource NoteListBox}"
                 VirtualizingPanel.ScrollUnit="Pixel"
                 AllowDrop="True"
                 PreviewMouseLeftButtonDown="NoteList_PreviewMouseLeftButtonDown"
                 PreviewMouseMove="NoteList_PreviewMouseMove"
                 Drop="NoteList_Drop">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="CardBorder"
                                        Style="{StaticResource NoteCard}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Row="0" Grid.Column="0"
                                                   Text="{Binding Title}"
                                                   Foreground="{StaticResource TextPrimary}"
                                                   FontSize="14"
                                                   FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis"
                                                   Margin="0,0,8,2"/>

                                        <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                                                    Orientation="Horizontal" VerticalAlignment="Center">
                                            <Button Style="{StaticResource IconButton}"
                                                    Command="{Binding DataContext.TogglePinCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                    CommandParameter="{Binding}"
                                                    Visibility="Collapsed"
                                                    x:Name="PinButton"
                                                    ToolTip="Pin/Unpin note"
                                                    Padding="4,6">
                                                <fa:IconBlock x:Name="PinIcon" Icon="Thumbtack" FontSize="12"
                                                              Foreground="{StaticResource TextMuted}"/>
                                            </Button>
                                            <Button Style="{StaticResource DangerButton}"
                                                    Margin="4,0,0,0"
                                                    Command="{Binding DataContext.DeleteNoteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                    CommandParameter="{Binding}"
                                                    Visibility="Collapsed"
                                                    x:Name="DeleteButton"
                                                    ToolTip="Delete note"
                                                    Padding="4,6">
                                                <fa:IconBlock Icon="TrashAlt" FontSize="12"/>
                                            </Button>
                                        </StackPanel>

                                        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                                   Text="{Binding ModifiedAt, StringFormat='{}{0:MMM d, yyyy  h:mm tt}'}"
                                                   Foreground="{StaticResource TextMuted}"
                                                   FontSize="11"/>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="CardBorder" Property="BorderBrush" Value="{StaticResource Accent}" />
                                        <Setter TargetName="PinButton" Property="Visibility" Value="Visible" />
                                        <Setter TargetName="DeleteButton" Property="Visibility" Value="Visible" />
                                    </Trigger>
                                    <DataTrigger Binding="{Binding IsPinned}" Value="True">
                                        <Setter TargetName="PinButton" Property="Visibility" Value="Visible" />
                                        <Setter TargetName="PinIcon" Property="Foreground" Value="{StaticResource Accent}" />
                                    </DataTrigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="CardBorder" Property="Background" Value="{StaticResource Accent}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</UserControl>
